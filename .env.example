# Django Configuration
SECRET_KEY=django-insecure-change-me-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0
DJANGO_SETTINGS_MODULE=file_vault.settings

# Database Configuration
DB_NAME=file_vault
DB_USER=root
DB_PASSWORD=password
DB_HOST=db
DB_PORT=3306
USE_SQLITE=False

# MySQL Container Configuration
MYSQL_ROOT_PASSWORD=password
MYSQL_DATABASE=file_vault

# Application Configuration
PYTHONDONTWRITEBYTECODE=1
PYTHONUNBUFFERED=1

# JWT Configuration (optional - using defaults in settings.py)
# JWT_ACCESS_TOKEN_LIFETIME_MINUTES=60
# JWT_REFRESH_TOKEN_LIFETIME_DAYS=7

# CORS Configuration (optional - using defaults in settings.py)
# CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Storage Configuration (for future use)
# MEDIA_ROOT=/app/media
# STATIC_ROOT=/app/staticfiles
